@AGENTS.md

# Comment style

Don't add comments by default. Only add one when the code isn't self-explanatory
(a non-obvious decision, a workaround, a gotcha). When you do, keep it to one or
two lines — no paragraph-style explanations.
